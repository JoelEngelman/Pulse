import { useCallback, useEffect, useState } from "react";
import { ArrowLeft, ImagePlus, Loader2, Send, Music2, BarChart3 } from "lucide-react";
import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { useGetMe } from "@workspace/api-client-react";
const API="https://pulse-api-proxy.joeldavidengelman.workers.dev";
async function api(path:string,options:RequestInit={}){const r=await fetch(`${API}${path}`,{...options,credentials:"include",headers:{"Content-Type":"application/json",...(options.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d;}
async function imageData(file:File){const b=await createImageBitmap(file);const max=1400,s=Math.min(1,max/Math.max(b.width,b.height)),c=document.createElement("canvas");c.width=Math.max(1,Math.round(b.width*s));c.height=Math.max(1,Math.round(b.height*s));const x=c.getContext("2d")!;x.drawImage(b,0,0,c.width,c.height);return c.toDataURL("image/jpeg",.8)}
export default function CreatorStudio(){const[,setLocation]=useLocation();const{data:user}=useGetMe();const[content,setContent]=useState("");const[image,setImage]=useState("");const[posts,setPosts]=useState<any[]>([]);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState("");const load=useCallback(async()=>{try{const all=await api("/api/feed?mode=for-you");setPosts(all.filter((p:any)=>String(p.user?.id)===String(user?.id)))}catch(e:any){setError(e.message)}finally{setLoading(false)}},[user?.id]);useEffect(()=>{if(user)load()},[user,load]);const publish=async()=>{if(!content.trim()&&!image)return;setSaving(true);setError("");try{const p=await api("/api/posts",{method:"POST",body:JSON.stringify({content:content.trim(),imageUrl:image||null})});setPosts(x=>[p,...x]);setContent("");setImage("")}catch(e:any){setError(e.message)}finally{setSaving(false)}};return <div className="h-full overflow-y-auto"><div className="max-w-3xl mx-auto px-4 py-5 md:py-8"><button onClick={()=>setLocation("/feed")} className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground mb-5 cursor-pointer"><ArrowLeft className="w-4 h-4"/>Back</button><div className="flex items-center gap-3 mb-7"><div className="w-11 h-11 rounded-2xl bg-primary/10 text-primary grid place-items-center"><BarChart3 className="w-6 h-6"/></div><div><h1 className="text-2xl font-bold">Creator Studio</h1><p className="text-sm text-muted-foreground">Create and manage your Pulse Social posts.</p></div></div><section className="bg-card border border-border rounded-2xl p-4 mb-6"><div className="flex items-center gap-3 mb-3"><Avatar className="w-10 h-10"><AvatarImage src={user?.avatarUrl||""}/><AvatarFallback>{user?.displayName?.slice(0,1)||"P"}</AvatarFallback></Avatar><div><p className="font-semibold">{user?.displayName}</p><p className="text-xs text-muted-foreground">@{user?.username}</p></div></div><Textarea value={content} onChange={e=>setContent(e.target.value)} maxLength={500} placeholder="Create something for Pulse…" className="min-h-28 resize-none"/>{image&&<img src={image} className="mt-3 max-h-80 w-full object-cover rounded-xl"/>}<div className="flex items-center gap-2 mt-3"><label className="cursor-pointer inline-flex items-center gap-2 px-3 py-2 rounded-xl hover:bg-secondary text-sm"><ImagePlus className="w-4 h-4"/>Photo<input type="file" accept="image/*" className="hidden" onChange={async e=>{const f=e.target.files?.[0];if(f)setImage(await imageData(f));e.currentTarget.value=""}}/></label><span className="inline-flex items-center gap-2 px-3 py-2 text-sm text-muted-foreground"><Music2 className="w-4 h-4"/>Music is available in Pulse Social</span><Button onClick={publish} disabled={saving||(!content.trim()&&!image)} className="ml-auto cursor-pointer">{saving?<Loader2 className="w-4 h-4 mr-2 animate-spin"/>:<Send className="w-4 h-4 mr-2"/>Publish</Button></div>{error&&<p className="text-sm text-destructive mt-3">{error}</p>}</section><div className="flex items-center justify-between mb-3"><h2 className="font-semibold">Your posts</h2><span className="text-sm text-muted-foreground">{posts.length} loaded</span></div>{loading?<Loader2 className="animate-spin"/>:posts.map(p=><article key={p.id} className="bg-card border border-border rounded-2xl p-4 mb-3"><p className="text-sm whitespace-pre-wrap">{p.content}</p>{p.media?.image&&<img src={p.media.image} className="mt-3 max-h-80 w-full object-cover rounded-xl"/>}<p className="text-xs text-muted-foreground mt-3">{new Date(p.createdAt).toLocaleString()} · {p.likeCount||0} likes · {p.commentCount||0} comments</p></article>)}</div></div>}
