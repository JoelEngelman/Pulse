import { useEffect, useMemo, useState } from "react";
import { Activity, ArrowLeft, BarChart3, Clock3, MessageSquare, RefreshCw, Users, Zap, UserPlus, CalendarDays } from "lucide-react";
import { useLocation } from "wouter";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/hooks/use-auth";
import { getInitials } from "@/lib/utils";

const API="https://lbphvoonoxpbvpovozuo.supabase.co/functions/v1/pulse-account";
const TOKEN="pulse-supabase-access-token";
const OWNER="joeldavidengelman";
type Person={id:string;username:string;displayName:string;avatarUrl?:string|null;isOnline:boolean;lastSeen:string|null;createdAt:string;postCount:number;messageCount:number;accountAgeDays:number};
type Analytics={generatedAt:string;stats:{users:number;activeUsers:number;active24h:number;active7d:number;posts:number;messages:number;conversations:number;postsPerUser:number;messagesPerUser:number};people:Person[]};
function relative(value:string|null){if(!value)return "Never";const ms=Date.now()-new Date(value).getTime();if(ms<60000)return "Just now";if(ms<3600000)return `${Math.floor(ms/60000)}m ago`;if(ms<86400000)return `${Math.floor(ms/3600000)}h ago`;return new Date(value).toLocaleString();}
function accountAge(days:number){if(days<1)return "Today";if(days<30)return `${days}d`;if(days<365)return `${Math.floor(days/30)}mo`;return `${Math.floor(days/365)}y`}
export default function Admin(){const[,setLocation]=useLocation();const{user}=useAuth();const[data,setData]=useState<Analytics|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[query,setQuery]=useState("");const[sort,setSort]=useState<"activity"|"posts"|"messages"|"joined">("activity");
const load=async()=>{setLoading(true);setError("");try{const token=localStorage.getItem(TOKEN);const r=await fetch(`${API}/admin/analytics`,{headers:{Authorization:`Bearer ${token}`}});const d=await r.json();if(!r.ok)throw new Error(d.error||"Could not load analytics");setData(d);}catch(e:any){setError(e.message||"Could not load analytics.");}finally{setLoading(false);}};
useEffect(()=>{if(user?.username?.toLowerCase()!==OWNER){setLocation("/feed");return;}load();},[user?.username]);
const people=useMemo(()=>{const q=query.trim().toLowerCase();const list=(data?.people??[]).filter(p=>!q||`${p.username} ${p.displayName}`.toLowerCase().includes(q));return [...list].sort((a,b)=>sort==="posts"?b.postCount-a.postCount:sort==="messages"?b.messageCount-a.messageCount:sort==="joined"?new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime():(new Date(b.lastSeen||0).getTime()-new Date(a.lastSeen||0).getTime()));},[data,query,sort]);
if(user?.username?.toLowerCase()!==OWNER)return null;
return <div className="h-screen min-h-0 overflow-y-auto overscroll-contain"><div className="max-w-7xl mx-auto px-4 py-6 md:py-10 space-y-6 pb-16"><div className="flex items-center justify-between gap-3"><div><button onClick={()=>setLocation("/feed")} className="text-sm text-muted-foreground hover:text-foreground inline-flex items-center gap-2 mb-3"><ArrowLeft className="w-4 h-4"/>Back</button><div className="flex items-center gap-3"><div className="rounded-2xl bg-primary/10 text-primary p-3"><BarChart3 className="w-6 h-6"/></div><div><h1 className="text-3xl font-bold tracking-tight">Pulse Analytics</h1><p className="text-sm text-muted-foreground">Private owner dashboard · your activity is excluded.</p></div></div></div><Button variant="outline" onClick={load} disabled={loading}><RefreshCw className={`w-4 h-4 mr-2 ${loading?"animate-spin":""}`}/>Refresh</Button></div>
{error&&<div className="rounded-2xl border border-destructive/30 bg-destructive/10 text-destructive p-4">{error}</div>}
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3">{[[Users,"Users",data?.stats.users??0],[Activity,"Active now",data?.stats.activeUsers??0],[BarChart3,"Posts",data?.stats.posts??0],[MessageSquare,"Messages",data?.stats.messages??0],[Zap,"Conversations",data?.stats.conversations??0]].map(([Icon,label,value]:any)=><div key={label} className="rounded-2xl border border-border bg-card/70 p-4"><Icon className="w-5 h-5 text-primary mb-3"/><p className="text-2xl font-bold">{value}</p><p className="text-xs text-muted-foreground mt-1">{label}</p></div>)}</div>
<div className="grid grid-cols-2 md:grid-cols-4 gap-3">{[[Activity,"Active in 24h",data?.stats.active24h??0],[Activity,"Active in 7 days",data?.stats.active7d??0],[BarChart3,"Posts / user",data?.stats.postsPerUser??0],[MessageSquare,"Messages / user",data?.stats.messagesPerUser??0]].map(([Icon,label,value]:any)=><div key={label} className="rounded-2xl border border-border bg-secondary/20 p-4"><Icon className="w-4 h-4 text-muted-foreground mb-2"/><p className="text-xl font-bold">{value}</p><p className="text-xs text-muted-foreground">{label}</p></div>)}</div>
<div className="rounded-2xl border border-border bg-card/70 overflow-hidden"><div className="p-5 border-b border-border"><div className="flex flex-col xl:flex-row xl:items-center gap-3"><div className="flex-1"><h2 className="font-semibold text-lg">Everyone on Pulse</h2><p className="text-sm text-muted-foreground">All other accounts, with activity, content and account-age details.</p></div><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Filter users…" className="h-10 rounded-xl border border-border bg-background/60 px-3 text-sm outline-none xl:w-64"/></div><div className="flex flex-wrap gap-2 mt-4"><span className="text-xs text-muted-foreground self-center mr-1">Sort:</span>{([["activity","Last active"],["posts","Most posts"],["messages","Most messages"],["joined","Newest accounts"]] as const).map(([id,label])=><button key={id} onClick={()=>setSort(id)} className={`rounded-xl px-3 py-1.5 text-xs border transition-colors ${sort===id?"bg-primary text-primary-foreground border-primary":"border-border hover:bg-secondary"}`}>{label}</button>)}</div></div>{loading&&!data?<div className="p-12 text-center text-muted-foreground">Loading analytics…</div>:people.length===0?<div className="p-12 text-center text-muted-foreground">No users match.</div>:<div className="divide-y divide-border">{people.map(p=><div key={p.id} className="p-4 flex flex-col lg:flex-row lg:items-center gap-4 hover:bg-secondary/20 transition-colors"><div className="flex items-center gap-3 min-w-0 lg:w-[32%]"><Avatar className="w-11 h-11 shrink-0"><AvatarImage src={p.avatarUrl||""}/><AvatarFallback>{getInitials(p.displayName)}</AvatarFallback></Avatar><div className="min-w-0"><div className="flex items-center gap-2"><p className="font-semibold truncate">{p.displayName}</p><span className={`w-2 h-2 shrink-0 rounded-full ${p.isOnline?"bg-emerald-500":"bg-muted-foreground/40"}`}/></div><p className="text-sm text-muted-foreground truncate">@{p.username}</p></div></div><div className="grid grid-cols-3 gap-3 flex-1"><div className="rounded-xl bg-secondary/30 px-3 py-2"><p className="text-sm font-semibold">{p.postCount}</p><p className="text-[11px] text-muted-foreground">Posts</p></div><div className="rounded-xl bg-secondary/30 px-3 py-2"><p className="text-sm font-semibold">{p.messageCount}</p><p className="text-[11px] text-muted-foreground">Messages sent</p></div><div className="rounded-xl bg-secondary/30 px-3 py-2"><p className="text-sm font-semibold">{accountAge(p.accountAgeDays)}</p><p className="text-[11px] text-muted-foreground">Account age</p></div></div><div className="lg:w-[28%] lg:text-right"><p className="text-sm font-medium">{relative(p.lastSeen)}</p><p className="text-xs text-muted-foreground lg:flex lg:items-center lg:justify-end gap-1 mt-1"><Clock3 className="w-3 h-3 inline"/> {p.lastSeen?new Date(p.lastSeen).toLocaleString():"No activity yet"}</p><p className="text-xs text-muted-foreground mt-1"><CalendarDays className="w-3 h-3 inline mr-1"/>Joined {new Date(p.createdAt).toLocaleDateString()}</p></div></div>)}</div>}</div><p className="text-xs text-muted-foreground text-center">Last refreshed {data?.generatedAt?new Date(data.generatedAt).toLocaleString():"—"} · Owner account excluded from every number above.</p></div></div>;
}
