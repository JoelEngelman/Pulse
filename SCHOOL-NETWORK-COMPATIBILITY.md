# Pulse network compatibility

Pulse uses standard HTTPS requests on port 443 and HTTP polling for realtime updates.

This improves compatibility with restrictive networks but does not bypass school or organisation network controls.
