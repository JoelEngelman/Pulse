# Pulse API proxy

Placeholder for a same-origin proxy architecture. GitHub Pages itself cannot execute server-side proxy code; use the Cloudflare Worker for `/api/*` routing when a same-origin hostname is available.
